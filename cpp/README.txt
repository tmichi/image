コンパイル方法

1) UNIX-lineなシステムの場合
以下をタイプする．
   $ make 
   $ make check 
negate.bmpと呼ばれるファイルが出来ていればok.

2) Visual studioの場合
   1) image.slnを開く．
   2) ビルドする．
   3) sample.exeファイルをsample.bmpとtest.batがあるフォルダにいれて test.batをダブルクリックする．

